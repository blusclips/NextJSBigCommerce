/** @format */

import React from 'react';
import { render, fireEvent } from '@testing-library/react';

import IndexScreen from './index';

describe('<IndexScreen />', () => {
	test('should display a white screen with a templary table', async () => {
		// ???
	});
});
